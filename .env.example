# 模型配置已改为在页面右上角「模型配置」中填写，保存于浏览器 localStorage。
# 服务端不持有任何 API Key，因此本文件无需配置模型相关变量。

# ---- 插图（文生图）----
# 默认用 Pollinations 免费服务，无需 key。通常无需修改
# IMAGE_BASE_URL=https://image.pollinations.ai/prompt
# IMAGE_MODEL=flux
